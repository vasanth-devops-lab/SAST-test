[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "my-python-calculator"
version = "1.0.0"
description = "Simple Python calculator package"
authors = [
    {name = "Developer"}
]
requires-python = ">=3.8"